<div>
  <div class="recipe-container">
    <div class="recipeHeader">
      <h1> My Recipes </h1>
      <div class="addRecipeBtn">
        <%= link_to "New Recipe", new_recipe_path, class: "btn btn-primary" %>
      </div>
    </div>
    <hr>
<<<<<<< .merge_file_N4Mser
    <ul class="p-0">
      <% @recipes.includes(:user).each do |recipe| %>
        <li class="card p-3 row flex-row mb-3">
          <div class="col-3 h3 text-center">
            <%= link_to recipe.name, recipe_path(recipe), class: 'text-decoration-none d-block pt-3 text-start' %>
=======
    <ul>
      <% @recipes.each do |recipe| %>
        <li class="recipeCont">
          <div class="recipeLeft">
            <%= link_to recipe.name, recipe_path(recipe), class: 'recipeTitle' %>
            <% if recipe.user == current_user %>
              <%= link_to 'remove', recipe_path(recipe), data: { turbo_method: :delete, confirm: 'Are you sure you want to remove this recipe?' }, class: 'removeBtn' %>
            <% end %>
>>>>>>> .merge_file_wjRHGe
          </div>
          <div class="recipeRight col-9">
            <p class="recipeDesc"><strong>Description:</strong> <%= recipe.description %></p>
          </div>
          <div>
            <% if recipe.user == current_user %>
            <%= link_to 'remove', recipe_path(recipe), data: { turbo_method: :delete, confirm: 'Are you sure you want to remove this recipe?' }, class: 'removeBtn btn btn-outline-primary btn-sm' %>
            <% end %>
          </div>
        </li>
      <% end %>
    </ul>
  </div>
</div>
